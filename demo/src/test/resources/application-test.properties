# Test configuration
spring.application.name=demo-test

# LLaMA Configuration for testing
llama.model.path=test-model.gguf
llama.max.prompt.length=1000
llama.generation.timeout.seconds=5

# Disable logging during tests
logging.level.root=WARN
logging.level.org.springframework=WARN
